[probe]
exec = true
write = true
open = false
connect = false

[runtime]
# Optional Docker runtime limits for the wrapped Linux container.
# Leave values commented out to use the engine defaults.
# cpus = 2.0
# memory = "4g"
# cpuset = "0-3"
# pids_limit = 512
